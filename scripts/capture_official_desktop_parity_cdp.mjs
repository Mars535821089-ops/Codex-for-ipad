#!/usr/bin/env node

// Permanently disabled: Codex for iPad verification must not launch, attach to,
// copy, re-sign, or manipulate any macOS Codex/ChatGPT application instance.
console.error("Desktop Codex parity capture is permanently disabled");
console.error("Codex for iPad validation must run on the physical iPad only");
process.exit(78);
